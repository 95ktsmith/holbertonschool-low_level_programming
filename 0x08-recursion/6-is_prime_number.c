#include "holberton.h"

/**
 * is_prime_number - is prime number
 * Description: Tests a number to see if it is prime
 * @n: Number
 * Return: 1 if prime, 0 otherwise.
 */

int is_prime_number(int n)
{
	n = 1;
	return (n);
}
